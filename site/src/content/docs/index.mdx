---
title: Connect your AI to Discord
description: Do real Discord community work through a caller-owned bot, with typed operations, explicit safety boundaries, and verifiable guild outcomes.
type: landing
template: splash
hero:
  tagline: Do real community work safely through a caller-owned bot. Plan high-risk guild changes, approve the exact target, and verify complete builds against Discord.
  actions:
    - text: Get a verified result
      link: /discord-mcp/start/activity-evidence/
      icon: right-arrow
      variant: primary
    - text: Set up discord-mcp
      link: /discord-mcp/start/
      icon: rocket
      variant: secondary
---

import { Badge, Card, CardGrid } from '@astrojs/starlight/components';
import ClientTabs from '../../components/docs/ClientTabs.astro';
import DocsCardGrid from '../../components/docs/DocsCardGrid.astro';

<p class="product-facts">
  <Badge text="Caller-owned bot" variant="success" />
  <Badge text="208 tools · 31 categories" variant="note" />
  <Badge text="stdio + authenticated HTTP" variant="tip" />
  <Badge text="Activity Evidence for guild builds" variant="success" />
  <Badge text="Pre-1.0" variant="caution" />
</p>

## From intent to verified Discord work

Your AI host owns the conversation, reasoning, and scheduling. discord-mcp owns the Discord
boundary: typed operations, exact bot and guild scope, resumable execution, and live readback.
It runs locally as a child process by default or as a bearer-protected Streamable HTTP endpoint.
There is no hosted bot relay and no separate discord-mcp account.

<CardGrid>
  <Card title="1. Observe through your bot" icon="discord">
    The caller supplies one dedicated Discord bot. Server-side allowlists bind operations to the
    intended guild while structured reads give the AI current Discord context.
  </Card>
  <Card title="2. Propose and approve" icon="approve-check-circle">
    Typed contracts constrain every call. Complete guild builds begin with a target-bound dry run
    and require approval of the exact bot, guild, plan, and operation summary.
  </Card>
  <Card title="3. Act, resume, and verify" icon="seti:json">
    Discord returns reusable structured data. Approved guild builds checkpoint every step and
    finish with Activity Evidence backed by an independent live readback.
  </Card>
</CardGrid>

## Get connected

The tutorial covers bot creation, CLI installation, guided setup, and client configuration. Its
connection track begins with a read-only identity check. An optional final unit then walks through
one complete plan, approval, apply, resume, and Activity Evidence lifecycle in a private test guild.

<ClientTabs>
  <div slot="claude-desktop">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client claude-desktop
  ```

  </div>
  <div slot="claude-code">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client claude-code
  ```

  </div>
  <div slot="codex">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client codex
  ```

  </div>
  <div slot="gemini-cli">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client gemini-cli
  ```

  </div>
  <div slot="cursor">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client cursor
  ```

  </div>
  <div slot="generic">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client generic
  ```

  </div>
</ClientTabs>

Requires Node.js 22.12 or newer. You also need a Discord bot installed in a test server.
[Follow the setup tutorial →](/discord-mcp/start/) or
[go directly to the verified-outcome track →](/discord-mcp/start/activity-evidence/)

## Inspect before connecting

You can validate the real MCP catalog before creating or configuring a Discord bot:

```bash
discord-mcp catalog --check
# or: discord-mcp catalog --check --json
```

This check needs no Discord token, performs no Discord request or write, and does not
count as Activity Evidence. It only confirms that the installed package exposes the real
catalog contract. When it passes, continue with the [setup tutorial →](/discord-mcp/start/)
to connect a caller-owned bot and reach a verified Discord outcome.

## Start from your goal

<DocsCardGrid
  links={[
    { title: 'Get a verified Discord result', href: '/discord-mcp/start/activity-evidence/', description: 'Turn one request into a reviewed guild plan, resumable apply, and independently revalidated Activity Evidence.' },
    { title: 'Connect for the first time', href: '/discord-mcp/start/', description: 'Create a least-privilege Discord bot, install the CLI, connect an AI client, and prove the real MCP path.' },
    { title: 'Find the right tool', href: '/discord-mcp/tools/', description: 'Search 208 tool contracts by task, category, or technical identifier without expanding a giant sidebar.' },
    { title: 'Operate a community', href: '/discord-mcp/recipes/', description: 'Combine Discord operations for moderation, announcements, webhooks, intelligence, and live events.' },
    { title: 'Watch a real build', href: '/discord-mcp/showcase/live-gaming-server/', description: 'See an AI agent turn a fresh guild into a complete gaming community through a caller-owned bot.' },
    { title: 'Run it in production', href: '/discord-mcp/operations/', description: 'Configure least privilege, telemetry, retries, timeouts, and the documented audit boundaries.' },
  ]}
/>

## Know the safety boundary

discord-mcp cannot do more than the bot can do in Discord, so bot roles and channel
overrides are the primary access boundary. Tool annotations describe risk, but ordinary
writes can execute immediately. Only tools explicitly marked **Confirmation required** use
the two-part `__confirm` and `MCP_DRY_RUN` gate.

Before production use:

- grant the bot only the server and channel permissions required by the workflow;
- restrict enabled categories with `MCP_CATEGORIES`;
- understand which mutations are confirmation-gated;
- treat the current audit trail as an operational aid, not a blanket compliance guarantee;
- validate the exact release in a test guild because the project is still pre-1.0.

[Configure a safe baseline →](/discord-mcp/operations/configure/)

## Built for exact inspection

<CardGrid>
  <Card title="Searchable reference" icon="magnifier">
    Filter every tool by name, task, category, and risk. Leaf pages stay searchable without
    taking over navigation.
  </Card>
  <Card title="Source-linked contracts" icon="github">
    Generated pages link their Edit action to the TypeScript definition and expose the full
    input and output JSON Schemas.
  </Card>
  <Card title="Operational controls" icon="setting">
    OpenTelemetry, retries, timeouts, circuit breaking, bulkheads, category filtering, and
    audit sinks are configured through explicit environment variables.
  </Card>
  <Card title="Discord-native output" icon="discord">
    Components V2, webhooks, threads, roles, moderation, scheduled events, and other Discord
    surfaces return reusable structured fields.
  </Card>
</CardGrid>

Need help? Use [Troubleshooting](/discord-mcp/start/troubleshooting/), ask a redacted question in
[GitHub Discussions](https://github.com/cappyeo/discord-mcp/discussions/categories/q-a), or open a
[GitHub issue](https://github.com/cappyeo/discord-mcp/issues) with secrets removed. Share a
credential-safe result in [Show and tell](https://github.com/cappyeo/discord-mcp/discussions/categories/show-and-tell).

The optional `discord-mcp activity --report` shortcut only prints the fixed
credential-safe outcome-form URL. It never opens a browser, accesses the
network, or submits a report. Local activity remains on the operator's machine
and records only timestamps plus predefined command or blueprint-stage, status,
outcome, transport, and signal values. It contains no Discord IDs, names,
message content, tokens, paths, or raw errors.
