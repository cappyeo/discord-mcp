---
title: Discord tools for AI clients
description: Connect an MCP-compatible AI client to Discord through a typed, observable bot server running locally or over Streamable HTTP.
type: landing
template: splash
hero:
  tagline: Give your AI client a typed Discord toolset while keeping the bot token and server process under your control.
  actions:
    - text: Start the quickstart
      link: /discord-mcp/start/quickstart/
      icon: right-arrow
      variant: primary
    - text: Browse tools
      link: /discord-mcp/tools/
      icon: magnifier
      variant: secondary
---

import { Badge, Card, CardGrid } from '@astrojs/starlight/components';
import ClientTabs from '../../components/docs/ClientTabs.astro';
import DocsCardGrid from '../../components/docs/DocsCardGrid.astro';

<p class="product-facts">
  <Badge text="201 tools · 30 categories" variant="success" />
  <Badge text="stdio + Streamable HTTP" variant="note" />
  <Badge text="Structured results" variant="tip" />
  <Badge text="Pre-1.0" variant="caution" />
</p>

## From a prompt to Discord

discord-mcp runs locally as a child process by default. It can also expose a
bearer-protected Streamable HTTP endpoint for a remote MCP client. In either
case, the client discovers typed tools, sends structured arguments, and receives
machine-readable results after the server calls Discord's API. There is no hosted
relay and no separate account to create.

<CardGrid>
  <Card title="1. Your client chooses a tool" icon="seti:json">
    Tool descriptions and JSON Schemas tell the client when a tool fits and which Discord IDs
    it requires.
  </Card>
  <Card title="2. discord-mcp enforces the contract" icon="approve-check-circle">
    Validation, category controls, resilience, telemetry, and configured safety checks run
    before and around the Discord request.
  </Card>
  <Card title="3. Discord returns structured data" icon="discord">
    IDs, jump URLs, counts, and timestamps can feed the next tool call without being rebuilt
    from conversational prose.
  </Card>
</CardGrid>

## Get connected

Install the CLI, keep `DISCORD_TOKEN` in the launch environment, run guided setup, and merge
the generated snippet into the client's existing `mcpServers` object. The full quickstart
begins with a read-only identity check before any message is sent.

<ClientTabs>
  <div slot="claude-desktop">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client claude-desktop
  ```

  </div>
  <div slot="claude-code">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client claude-code
  ```

  </div>
  <div slot="codex">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client codex
  ```

  </div>
  <div slot="cursor">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client cursor
  ```

  </div>
  <div slot="generic">

  ```bash
  npm install -g @discord-mcp/cli
  discord-mcp setup --profile devbot --client generic
  ```

  </div>
</ClientTabs>

Requires Node.js 22.12 or newer. You also need a Discord bot installed in a test server.
[Follow the verified quickstart →](/discord-mcp/start/quickstart/)

## Start from your goal

<DocsCardGrid
  links={[
    { title: 'Connect for the first time', href: '/discord-mcp/start/', description: 'Create a least-privilege Discord bot, install the CLI, connect a client, and verify two real calls.' },
    { title: 'Find the right tool', href: '/discord-mcp/tools/', description: 'Search 201 tool contracts by task, category, or technical identifier without expanding a giant sidebar.' },
    { title: 'Build a workflow', href: '/discord-mcp/recipes/', description: 'Combine tools for moderation, rich messages, webhooks, intelligence, pipelines, and live events.' },
    { title: 'Run it in production', href: '/discord-mcp/operations/', description: 'Configure least privilege, telemetry, retries, timeouts, and the documented audit boundaries.' },
    { title: 'Connect OpenAI remotely', href: '/discord-mcp/operations/openai/', description: 'Publish a bearer-protected HTTPS MCP endpoint for the Responses API or Codex, with its shared-bot boundary explicit.' },
  ]}
/>

## Know the safety boundary

discord-mcp cannot do more than the bot can do in Discord, so bot roles and channel
overrides are the primary access boundary. Tool annotations describe risk, but ordinary
writes can execute immediately. Only tools explicitly marked **Confirmation required** use
the two-part `__confirm` and `MCP_DRY_RUN` gate.

Before production use:

- grant the bot only the server and channel permissions required by the workflow;
- restrict enabled categories with `MCP_CATEGORIES`;
- understand which mutations are confirmation-gated;
- treat the current audit trail as an operational aid, not a blanket compliance guarantee;
- validate the exact release in a test guild because the project is still pre-1.0.

[Configure a safe baseline →](/discord-mcp/operations/configure/)

## Built for exact inspection

<CardGrid>
  <Card title="Searchable reference" icon="magnifier">
    Filter every tool by name, task, category, and risk. Leaf pages stay searchable without
    taking over navigation.
  </Card>
  <Card title="Source-linked contracts" icon="github">
    Generated pages link their Edit action to the TypeScript definition and expose the full
    input and output JSON Schemas.
  </Card>
  <Card title="Operational controls" icon="setting">
    OpenTelemetry, retries, timeouts, circuit breaking, bulkheads, category filtering, and
    audit sinks are configured through explicit environment variables.
  </Card>
  <Card title="Discord-native output" icon="discord">
    Components V2, webhooks, threads, roles, moderation, scheduled events, and other Discord
    surfaces return reusable structured fields.
  </Card>
</CardGrid>

Need help? Use [Troubleshooting](/discord-mcp/start/troubleshooting/) or open a
[GitHub issue](https://github.com/cappyeo/discord-mcp/issues) with secrets removed.
