name: Verified outcome report
description: Share a credential-safe activation checkpoint or Discord outcome, first friction, and whether discord-mcp was useful enough to continue or reuse.
title: "[Outcome]: "
labels:
  - verified-outcome
body:
  - type: markdown
    attributes:
      value: |
        Thank you for helping validate discord-mcp with real use. Report only coarse, redacted facts. Never include or attach a bot token, authorization header, client config, webhook credential, screenshot, terminal log, exported file, Discord ID, server/channel name, username, message content, plan reference/token, plan ID, approval ID, evidence ID, or private Activity Evidence file.

        A public issue is optional and is not product telemetry. discord-mcp does not send this report from your installation.

  - type: dropdown
    id: outcome
    attributes:
      label: Furthest verified outcome
      description: Choose the furthest trustworthy state you observed.
      options:
        - Catalog check passed (no Discord access)
        - Activity Evidence returned verified
        - Apply completed but later evidence reported drift
        - Plan was ready but I did not apply it
        - Blocked before the plan became ready
        - Blocked during apply or resume
        - Blocked while reading Activity Evidence
        - I used a different Discord workflow without Activity Evidence
    validations:
      required: true

  - type: dropdown
    id: activation_stage
    attributes:
      label: Activation stage reached
      description: Choose the furthest privacy-safe stage you reached. A catalog check alone is not Activity Evidence.
      options:
        - CLI installed
        - Catalog check passed (no Discord access)
        - AI client connected
        - First read-only Discord check completed
        - Blueprint dry run ready
        - Approved apply completed
        - Activity Evidence returned verified
        - Reused a verified Discord workflow
    validations:
      required: true

  - type: dropdown
    id: ai_host
    attributes:
      label: AI host or MCP client
      options:
        - CLI only (no AI host)
        - Codex
        - Claude Desktop
        - Claude Code
        - Cursor
        - ChatGPT
        - Hermes Agent
        - Another MCP-compatible client
    validations:
      required: true

  - type: dropdown
    id: transport
    attributes:
      label: Connection mode
      options:
        - Not applicable - catalog check only
        - Local stdio
        - Streamable HTTP
        - Not sure
    validations:
      required: true

  - type: dropdown
    id: workflow
    attributes:
      label: Intended job
      description: What outcome or activation checkpoint were you trying to achieve?
      options:
        - Validate installation/catalog only
        - Build or redesign a server
        - Community briefing or unanswered-question review
        - Support triage
        - Moderation review
        - Announcement or event operation
        - Another Discord operation
    validations:
      required: true

  - type: dropdown
    id: time_to_outcome
    attributes:
      label: Time to the outcome above
      options:
        - Under 10 minutes
        - 10 to 30 minutes
        - 31 to 60 minutes
        - More than 60 minutes
        - I did not reach the intended outcome
    validations:
      required: true

  - type: dropdown
    id: repeat_use
    attributes:
      label: Repeat use
      description: This helps distinguish first-run curiosity from recurring value.
      options:
        - First day using discord-mcp
        - Used on 2 different days
        - Used on 3 or more different days
    validations:
      required: true

  - type: textarea
    id: result
    attributes:
      label: What happened?
      description: Describe the checkpoint or useful result without names, IDs, message content, or private server details.
      placeholder: The credential-free catalog check passed, or the agent completed a reviewed Discord workflow and independent evidence returned verified.
    validations:
      required: true

  - type: textarea
    id: first_friction
    attributes:
      label: First point of friction
      description: Name the first instruction, permission, client behavior, or tool result that slowed or blocked you. Write "None" if the path was clear.
    validations:
      required: true

  - type: textarea
    id: next_job
    attributes:
      label: What would make you continue or use it again next week?
      description: If you only checked the catalog, name what would help you connect a bot. Otherwise, prefer one recurring community job over a feature wishlist.
    validations:
      required: true

  - type: dropdown
    id: showcase_permission
    attributes:
      label: May the project quote this redacted report?
      description: Your issue is public either way; this controls reuse in project showcases or launch material.
      options:
        - No
        - Yes, with my GitHub handle
        - Yes, without naming my GitHub handle
    validations:
      required: true

  - type: checkboxes
    id: safety
    attributes:
      label: Public-report safety
      options:
        - label: I did not include any credential, client config, Discord ID, private name, message content, plan material, evidence ID, or Activity Evidence file.
          required: true
        - label: I did not attach a screenshot, terminal log, exported file, or other artifact containing private or identifying data.
          required: true
        - label: This report either covers the catalog-only check with no Discord access, or describes my own caller-controlled bot and an authorized Discord server.
          required: true
        - label: This is not a suspected security vulnerability or authorization-boundary failure; I will report those privately instead.
          required: true
